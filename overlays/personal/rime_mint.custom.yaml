patch:
  "schema/name": 拼音
  "menu/page_size": 9
  "punctuator/import_preset": symbols.custom
  "punctuator/full_shape":
    "/": { commit: ／ }
  "punctuator/half_shape":
    "/": { commit: "/" }
  "engine/processors/@before 0": lua_processor@*english_enter_learner
  "engine/translators/@before 0": lua_translator@*quick_text
  "engine/translators/@before 1": lua_translator@*english_learner
  "melt_eng/enable_user_dict": true
  "melt_eng/enable_completion": true
  "speller/algebra/@before 0": derive/^([zcs])h/$1/
  "speller/algebra/@before 1": derive/^([zcs])([^h])/$1h$2/
  "speller/algebra/@before 2": derive/([aei])n$/$1ng/
  "speller/algebra/@before 3": derive/([aei])ng$/$1n/
  "speller/algebra/@before 4": derive/^n/l/
  "speller/algebra/@before 5": derive/^l/n/
  "switches/@last":
     name: emoji_suggestion
     reset: 0
     states: [ "😣️","😁️"]
  'key_binder': # no import_preset
    bindings:
      # - {accept: "Control+Shift+E", toggle: emoji_suggestion, when: always}
      - {accept: "Control+Shift+exclam", toggle: transcription, when: has_menu}
      - {accept: "Control+Shift+1", toggle: transcription, when: has_menu}
      - {accept: minus, send: Page_Up, when: paging}     # 使用`-`进行向上翻页（第一页时候无效）
      - {accept: equal, send: Page_Down, when: has_menu} # 使用`=`进行向下翻页
      - { when: composing, accept: Control+p, send: Up }
      - { when: composing, accept: Control+n, send: Down }
      - { when: composing, accept: Control+b, send: Left }
      - { when: composing, accept: Control+f, send: Right }
      - { when: composing, accept: Control+a, send: Home }
      - { when: composing, accept: Control+e, send: End }
      - { when: composing, accept: Control+d, send: Delete }
      - { when: composing, accept: Control+k, send: Shift+Delete }
      - { when: composing, accept: Control+g, send: Escape }
      - { when: composing, accept: Alt+v, send: Page_Up }
      - { when: composing, accept: Control+v, send: Page_Down }
      - { when: composing, accept: ISO_Left_Tab, send: Shift+Left }
      - { when: composing, accept: Shift+Tab, send: Shift+Left }
      - { when: composing, accept: Tab, send: Shift+Right }
      - { when: paging, accept: minus, send: Page_Up }
      - { when: has_menu, accept: equal, send: Page_Down }
      - { when: paging, accept: comma, send: Page_Up }
      - { when: has_menu, accept: period, send: Page_Down }
      - { when: predicting, accept: comma, send: comma }
      - { when: predicting, accept: period, send: period }
      # - { when: always, accept: Control+Shift+1, select: .next }
      # - { when: always, accept: Control+Shift+2, toggle: ascii_mode }
      #- { when: always, accept: Control+Shift+3, toggle: full_shape }
      #- { when: always, accept: Control+Shift+4, toggle: simplification }
      #- { when: always, accept: Control+Shift+5, toggle: extended_charset }
      #- { when: always, accept: Shift+space, toggle: full_shape }
      - { when: always, accept: Control+period, toggle: ascii_punct }
